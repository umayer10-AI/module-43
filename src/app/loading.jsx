import React from 'react';

const loading = () => {
    return (
        <div>
            <span className="loading loading-spinner text-error"></span>
        </div>
    );
};

export default loading;